name: Build and package for windows

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Package Application
      uses: JackMcKew/pyinstaller-action-windows@v0.1.5
      with:
        path: .

    - uses: actions/upload-artifact@v2
      with:
        name: quiz
        path: dist/windows
